# V1.3.0 用户钱包功能 — 设计规划

## 概述

在 Yadea 海外租车用户端中新增 **用户钱包** 模块，支持充值余额与赠送余额双账户管理、流水查询、提现记录。钱包作为支付方式之一接入现有租车结算流程。

## 用户与场景

| 角色 | 场景 |
|------|------|
| 已登录租车用户 | 充值 → 使用钱包余额支付租金 |
| 已登录租车用户 | 查看充值余额与赠送余额明细 |
| 已登录租车用户 | 提现（余额退回） |
| 运营/客服 | 后台赠送余额操作（前端仅展示流水） |

## 功能清单

### P0 — 核心 ✅

- [x] **钱包首页** — 展示总余额、充值余额（含冻结）、赠送余额（含冻结）、快捷操作入口
- [x] **充值流程** — 选择金额 → 支付方式 → 完成充值 → 余额更新
- [x] **充值余额流水** — 流水号、类型（充值/提现/消费/退款）、金额、变动后余额、关联订单、操作人、备注、时间
- [x] **赠送余额流水** — 流水号、类型（充值/扣减/消费/退款）、金额、变动后余额、关联订单、操作人、备注、时间
- [ ] 提现记录 — 提现申请列表（状态、金额、时间）（目前不支持，设计如此）

### P1 — 重要 ✅

- [x] 钱包作为支付选项出现在租车确认页（`confirm-page`）
- [x] 余额不足时引导充值
- [x] 充值余额与赠送余额使用优先级规则（先用赠送余额，再用充值余额）
- [x] 空状态展示（无流水、无提现记录）

### P2 — 增强 ✅

- [x] 充值金额快捷选项（Rp 50,000 / Rp 100,000 / Rp 200,000 / Rp 500,000）+ 自定义输入
- [ ] 提现申请表单（银行信息、金额、提交）（目前不支持，设计如此）
- [ ] 余额变动动画（留待后续版本）
- [ ] 流水筛选（按类型、时间范围，留待后续版本）

## 数据模型

### 钱包账户

```
walletAccount: {
  rechargeBalance: number,       // 充值余额（可用）
  rechargeFrozen: number,        // 充值冻结金额
  bonusBalance: number,          // 赠送余额（可用）
  bonusFrozen: number,           // 赠送冻结金额
  totalBalance: number,          // 总余额（可用）
  totalFrozen: number,           // 总冻结金额
  lastUpdated: string            // ISO 时间
}
```

### 充值流水

```
rechargeLedger: {
  id: string,                    // 流水号
  type: 'recharge' | 'withdraw' | 'consume' | 'refund',
  amount: number,                // 变动金额（正/负）
  balanceAfter: number,          // 变动后余额
  relatedOrder: string | null,   // 关联订单号
  operator: string,              // 操作人
  remark: string,                // 备注
  createdAt: string              // ISO 时间
}
```

### 赠送流水

```
bonusLedger: {
  id: string,                    // 流水号
  type: 'recharge' | 'deduct' | 'consume' | 'refund',
  amount: number,                // 变动金额（正/负）
  balanceAfter: number,          // 变动后余额
  relatedOrder: string | null,   // 关联订单号
  operator: string,              // 操作人
  remark: string,                // 备注
  createdAt: string              // ISO 时间
}
```

### 提现记录

```
withdrawalRecord: {
  id: string,                    // 提现单号
  amount: number,                // 提现金额
  status: 'pending' | 'processing' | 'completed' | 'rejected',
  applyTime: string,             // 创建时间
  completedTime: string | null,  // 完成时间
  remark: string                 // 备注
}
```

## 页面与路由

所有钱包相关页面采用现有全屏覆盖模式（与 `showStore`、`showMyOrders` 等一致）。

| 页面 | 触发变量 | 入口 |
|------|----------|------|
| 钱包首页 | `showWallet` | 个人中心 → 钱包卡片 |
| 充值页 | `showTopup` | 钱包首页 → 充值按钮 |
| 充值余额流水 | `showRechargeLedger` | 钱包首页 → 充值余额区块 |
| 赠送余额流水 | `showBonusLedger` | 钱包首页 → 赠送余额区块 |
| 提现记录 | `showWithdrawRecords` | 钱包首页 → 提现记录入口 |
| 提现申请 | `showWithdrawApply` | 提现记录页 → 申请提现按钮 |

## 交互流程

### 充值流程

```
钱包首页 → 点击「充值」→ 充值页（选择金额 + 支付方式）→ 确认充值 → 模拟支付 → 成功 → 返回钱包首页（余额更新）
```

### 余额使用优先级（支付时）

```
赠送余额（可用）→ 赠送余额不足时使用充值余额（可用）→ 仍不足则引导其他支付方式
```

### 流水查看

```
钱包首页 → 点击充值余额区域 → 充值流水列表（按时间倒序）
钱包首页 → 点击赠送余额区域 → 赠送流水列表（按时间倒序）
```

## 与现有系统的集成

### 导航集成
- 个人中心（`profile-page`）现有菜单项下方新增「我的钱包」入口
- 钱包首页顶部保留返回按钮，符合现有导航模式

### 支付集成
- 租车确认页（`confirm-page`）支付方式选项中新增「钱包」
- 钱包时计算可用余额，展示支付金额分配（赠送余额 + 充值余额）

### 多语言
- 在 `locales/*.json` 中新增 `wallet` 命名空间

## 视觉设计要点

- 沿用现有设计系统 token（`--accent:#2B3F5F`、`--accent-2:#FF6A00` 等）
- 钱包首页采用卡片式布局，突出余额数字
- 充值余额与赠送余额分两个卡片展示，冻结部分用 `--muted` 色标注
- 流水列表采用现有订单列表样式（`order-list-page` 的列表模式）
- 充值页金额选择使用网格按钮布局

## 冻结内容

以下内容保持 V1.2.0 冻结状态，不做任何改动：
- 现有页面结构（首页、车辆、个人中心、租车流程等 20+ 页面）
- 导航结构（底部三 Tab 及切换逻辑）
- 现有 Demo 操作面板
- 现有语种文件结构（`wallet` 命名空间为新增，不修改已有 key）
- 现有设计 Token 与全局样式

## 确认的设计决策

以下问题已由用户确认：

| 问题 | 决策 |
|------|------|
| 充值金额选项是否需要自定义输入？ | **支持** — 快捷选项 + 自定义输入 |
| 提现流程是否需要在当前版本实现完整提现申请表单？ | **需要** — 完整提现申请表单 + 提现记录列表 |
| 是否需要首页展示钱包余额的小组件？ | **不需要** |
| 是否需要支付密码/二次验证？ | **不需要** |

## 下一步

设计决策已全部确认。请审阅更新后的完整规划，确认后我将进入 V1.3.0 钱包功能的设计实现阶段。