# V1.31.0 换电功能 — 设计规划

## 概述

在 Yadea 海外租车用户端首页新增 **换电** 入口，为已绑定电池的用户提供换电站查找、到店换电的完整流程。换电完成后更新设备电池信息（SOC → 100%、换新电池 ID），并与现有设备体系（`homeBatteries`、电池详情页）打通。

## 用户与场景

| 角色 | 场景 |
|------|------|
| 已登录租车用户（已绑定电池） | 首页换电入口 → 查找附近换电站 → 到店换电 |
| 已登录租车用户（未绑定电池） | 首页看到换电入口但提示先绑定电池 |
| 已登录租车用户（电量低） | 电池 Tab 电量 ≤20% 时首页入口显示「电量低」警示，引导换电 |
| 运营/客服 | 换电站数据为模拟静态数据（前端演示） |

## 功能清单

### P0 — 核心

- [ ] **首页换电入口** — 租赁入口卡片改为左右双入口：`租赁` / `换电`，等高同权重
- [ ] **换电站列表页** — 站名、距离、营业状态、可用柜格数、搜索 + 列表/地图切换
- [ ] **换电站详情页** — 柜格分布、费用说明、导航到店（复用现有导航）、开始换电
- [ ] **换电流程** — 确认旧电池 → 模拟开柜 → 放入旧电池 → 取出新电池 → 绑定成功
- [ ] **换电完成联动** — 首页 `homeBatteries` 对应电池 SOC 更新为 100%、电池 ID 换新、toast 成功

### P1 — 重要

- [ ] 低电量警示：已绑定电池 SOC ≤20% 时，换电入口冒红色「电量低」徽标
- [ ] 未绑定电池空态：点击换电入口提示先绑定电池，可跳转绑定流程
- [ ] 换电站空态：无搜索结果 / 无可用柜格时的空状态展示
- [ ] 换电流程中模拟接口加载（复用 `showRentLoading` + `yadi-loading`）

### P2 — 增强

- [ ] 换电站收藏（附近常用站点）
- [ ] 换电历史记录
- [ ] 换电站柜格实时占用模拟刷新
- [ ] 换电完成后的电量环动画（留待后续版本）

## 数据模型

### 换电站

```
swapStation: {
  id: string,                  // 站点 ID
  name: string,                // 站点名称
  address: string,             // 地址
  distance: number,            // 距离（km）
  status: 'open' | 'closed',   // 营业状态
  availableSlots: number,      // 可用柜格数
  totalSlots: number,          // 总柜格数
  fees: string,                // 费用说明
  lat: number,                 // 纬度
  lng: number                  // 经度
}
```

### 电池（换电后更新）

```
homeBatteries[].soc: number    // 换电后 → 100
homeBatteries[].id: string     // 换电后 → 新电池 ID
homeBatteries[].model: string  // 换电后 → 新电池型号
```

## 页面与路由

所有换电相关页面采用现有全屏覆盖模式（与 `showRent`、`showStore`、`showBatteryDetail` 等一致）。

| 页面 | 触发变量 | 入口 |
|------|----------|------|
| 换电站列表 | `showSwapStations` | 首页 → 换电入口卡片 |
| 换电站详情 | `showSwapStation` | 换电站列表 → 选中站点 |
| 换电确认 | `showSwapConfirm` | 换电站详情 → 开始换电 |
| 换电进度 | `showSwapProgress` | 换电确认 → 确认换电 |

## 交互流程

### 首页换电入口（方案 A）

```
首页 → 点击「换电」卡片
  ├─ 未绑定电池 → toast 提示 + 引导绑定（跳转扫描绑定流程）
  └─ 已绑定电池 → 打开换电站列表（就近站点优先，可用柜格 ≥1）
```

### 换电流程

```
换电站详情 → 开始换电 → 换电确认（展示旧电池信息 + 费用）→ 确认
  → 模拟开柜 → 放入旧电池 → 取出新电池 → 绑定成功
  → 返回首页（homeBatteries 对应电池 SOC=100、ID 换新，toast 成功）
```

## 与现有系统的集成

### 导航集成
- 首页租赁入口卡片（`home-rental-entry`，`index.html:715`）由单卡改为双卡并排，右侧新增换电卡
- 换电站列表/详情顶部保留返回按钮，符合现有导航模式

### 设备体系集成
- 复用电池 Tab 数据源 `homeBatteries` 与 SOC 电量环
- 换电完成后同步更新 `homeBatteries`，电池详情页（`battery-detail-page`）数据一致
- 复用现有全屏覆盖 + `show*` 触发变量模式

### 模拟接口与加载规范
- 换电流程各步骤用 `showRentLoading` + `yadi-loading` 模拟接口，与骨架屏、下拉刷新指示器互斥（loading-mutual-exclusion）
- 换电站列表支持下拉刷新，沿用 `initPullToRefresh` 模式

### Demo 操作面板
- **demo-ops-per-page**：首页换电入口 ops（未登录 / 低电量 / 空态）与换电站页 ops 独立定义，不继承上级页面操作区块

### 多语言
- 在 `locales/*.json` 中新增 `swap` 命名空间（zh-CN / zh-HK / id / en 四语同步）

## 视觉设计要点

- 沿用现有设计系统 token（`--accent:#2B3F5F`、`--accent-2:#FF6A00`、`--bg:#F6F7F9`、`--surface:#FFFFFF`、`--muted:#999999`、`--border:#E5E5E5`）
- 换电卡用 `#FF6A00` 品牌渐变区分黑色租赁卡，图标用 `battery-charging` 风格
- 已绑定电池时卡片实时显示电量；SOC ≤20% 冒红色「电量低」徽标，做成「活入口」
- 换电站卡片列表沿用现有门店列表样式；地图切换复用首页 `mapCtx` 地图体系

## 冻结内容

以下内容保持 V1.30.0 冻结状态，不做任何改动：
- 现有页面结构（首页、车辆、个人中心、租车流程、钱包等 20+ 页面）
- 导航结构（底部三 Tab 及切换逻辑）
- 现有 Demo 操作面板
- 现有语种文件结构（`swap` 命名空间为新增，不修改已有 key）
- 现有设计 Token 与全局样式
- 现有设备绑定/解绑、车辆控制等既有流程

## 待确认的设计决策

| 问题 | 建议 | 待确认 |
|------|------|--------|
| 首页入口形态 | 方案 A：租赁卡改为双卡并排（`租赁` / `换电`） | 或方案 B 服务宫格 |
| 换电费用 | 换电模拟费用展示（如免费 / 按次计费） | 需确认计费口径 |
| 换电是否需要支付环节 | 复用钱包余额支付 或 免费换电 | 需确认是否联动钱包 |
| 未绑定电池时入口行为 | 提示先绑定电池并引导 | 需确认 |
| 换电站数据 | 纯前端静态模拟数据 | 需确认数量与城市 |

## 下一步

规划待确认后，进入 V1.31.0 换电功能设计实现阶段（变更影响评估 → 冻结内容 → 增量修改记录）。
