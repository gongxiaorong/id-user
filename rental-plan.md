# V1.4.0 用户端租车延展功能 — 设计规划（续租 / 支付 / 改价确认 / 退款查看）

> 文档版本：V1.1 · 2026-09-05
> 状态：草稿，待评审
> 依据：平台侧 `prototype-admin` 文档（`car-rental-rental-extensions-adr.md` 及 `docs/adr/0001~0005`）
> 范围：仅**用户端（C 端）小程序/APP** 的延展功能；后台逻辑以上述 ADR 为唯一真源
> 数据：全部 mock（不接接口），字段与流程对齐平台 ADR

---

## 0. 本次关键设计决策变更（用户确认）

平台侧 ADR 的默认模型在用户端做了两处收敛，以下为**已确认变更**，全文据此落地：

| # | 平台侧默认（ADR） | 用户端收敛（本次） | 影响范围 |
|---|---|---|---|
| **变更1** | D2：续租双模式 `USER_APPLY`（用户申请**需审核** PENDING/APPROVED/REJECTED）+ `AGENT`（客服代客） | **续租免审 · 可选租赁方案**：`USER_APPLY` 去掉审核态；用户可像下单一样选择「租赁方案」（`showPeriodPicker`/`showDatePicker`，默认沿用主单方案，可改），确认 → 直接生成续租子单（`UNPAID`）→ 跳转支付 → `PAID` | 续租流程、续租单状态机、续租方案选择器 |
| **变更2** | D9 / ADR-0001：组合支付，一笔订单可拆多条支付记录（钱包+2C2P+线下，各分摊金额） | **下单单一支付方式**：用户端支付为单渠道 `radio` 三选一（2C2P / 钱包余额 / 线下支付），**不跨渠道拆分配额** | 支付页、立即支付、续租支付 |

> 说明：变更2 不废除平台「组合支付」模型——组合支付是**后台支付拆分/改价**的口径；用户端仅约束「用户下单时不自行拆分配额」，由平台侧决定最终渠道分配。二者在"用户不手动拆分"这一点上收敛一致。

---

## 1. 概述

在 Yadea 海外租车**用户端**补齐租车订单的延展资金能力，覆盖订单从「待支付 → 使用中 → 结束」全周期的资金交互：

- **续租**（变更1 免审·可选方案）：使用中订单续租，用户可像下单一样选择租赁方案 → 确认即生成续租子单并进入支付。
- **支付**（变更2 单渠道）：用户选单一支付方式付清应付（含主单与续租单）。
- **改价确认**：平台改价后推送 APP 消息，用户端查看改价前后并「立即支付」。
- **退款查看**：平台发起退款（结束订单仅退押金），用户端仅查看，不可操作。
- **自动续租开关**：订单维度 `opt-in`，默认关闭（D18 系列）。

钱包能力沿用 `wallet-plan.md`（已实现），本规划仅做一致性核查（见 §7）。

---

## 2. 与平台 ADR 的对应关系

| 用户端功能 | 平台 ADR 真源 | 用户端差异 |
|---|---|---|
| 续租子单模型 | D3 / D12 / D16（独立子单 `parentOrderNo`，仅支付状态流转，复用主单押金，可多次平级） | 变更1：去掉 `USER_APPLY` 审核态；保留用户可选租赁方案 |
| 自动续租 | D18 / D18b / D18c / D18d（AUTO 模式，订阅式循环，扣款失败自动取消，不收押金，opt-in 默认关） | 一致，仅暴露开关与提醒 |
| 支付方式 | D9 / ADR-0001（组合支付） | 变更2：单渠道选择 |
| 改价（平台发起） | ADR-0001 / ADR-0002 / ADR-0004（改价=价格+渠道分配，记录前后快照 `paySplitAudits`） | C 端只读快照 + 「立即支付」 |
| 退款 | ADR-0003（按原渠道退、双分区、多次部分退）/ ADR-0005（结束订单仅退押金） | C 端仅查看记录 |
| 钱包抵扣 | D7（先充值后赠送）/ D13（提现仅充值余额）/ 冻结中间态不记流水 | 见 §7 一致性问题 |

---

## 3. 功能清单

### P0 — 核心

- [ ] **费用明细 · 列表承载** — 订单详情**不内联罗列**；新增「费用明细」入口 → 独立列表页（**参考商户端 `feedetail` 子页**），主单 + 各续租单以卡片列表呈现（租赁方案/租赁时间/租金/押金/合计/支付状态），底部汇总（租金/押金/合计）（D17）
- [ ] **订单详情 · 续租记录** — 主单 + 续租单平级列表，每行：类型/订单号/租赁方案/租赁时间(起~止)/支付状态/操作
- [ ] **续租（免审 · 可选租赁方案）** — 使用中订单 → 续租 → 像下单一样选「租赁方案」（默认主单方案，可改）→ 确认生成续租子单 → 支付（变更1）
- [ ] **支付（单渠道）** — 支付方式 `radio` 三选一 → 确认 → 模拟支付 → 成功（变更2）
- [ ] **改价确认** — 接收「订单已改价」推送 → 查看改价前后 → 「立即支付」（仅 `CREATED`）
- [ ] **退款查看** — 退款记录列表（租金/押金分区 + 渠道明细），结束订单仅退押金（ADR-0005）
- [ ] **自动续租开关** — 订单维度 `opt-in`，默认关闭；扣款前提醒（D18d）

### P1 — 重要

- [ ] 续租单「查看」详情（改价记录 / 支付状态）
- [ ] 钱包余额不足时支付页禁用钱包选项并引导充值
- [ ] 改价推送的「立即支付」仅覆盖线上应付部分（线下挂账由平台已挂账，不计入 C 端支付额，见 §4.3）
- [ ] 扣款失败自动续租取消的 toast 提示（D18c）

### P2 — 增强（可选）

- [ ] 续租历史的独立列表入口
- [ ] 退款进度的状态徽标（退款中 / 已退款）

---

## 4. 详细交互流程

### 4.1 续租流程（变更1：免审 · 可选租赁方案）

```
订单详情（使用中：IN_USE / TO_RETURN / 逾期 / 违约 等）
  → 点击「续租」（手动续租，区别于自动续租开关）
  → 【变更1：免审 + 用户可选租赁方案】
  → 续租方案选择层（复用下单的「租赁方案/租赁类型」选择器
        showPeriodPicker / showDatePicker，默认选中主单原方案，
        可像下单一样改选 时/日/周/月 租等不同方案）
  → 实时展示「续租租金 Rp {按所选方案计算的当期租金}」
  → 确认
  → 【变更1：跳过审核态 PENDING/APPROVED/REJECTED】
  → 生成续租子单（renewMode=USER_APPLY, parentOrderNo=主单号,
       newRentStart=主单 rentEnd,
       newRentEnd=rentEnd + 所选方案周期（周期由用户所选方案决定）,
       renewRentFee=所选方案当期租金, renewDeposit=0 复用主单押金）
  → 续租子单状态 = UNPAID
  → 跳转支付页（见 4.2）
  → 支付成功 → 续租子单 PAID → toast「续租成功」
```

- **续租单状态机（用户端）**：仅支付状态 `UNPAID → PAID`（D12/D16），不套用主单状态流转。
- **可选租赁方案（替代原"免填表"）**：续租不再"周期自动沿用、无选择"，而是复用下单方案选择器，用户可改选方案；申请理由字段仍移除（无需填写任何说明字段）。
- **多次平级续租**：主单下可生成多个续租单（均直接关联主单，D16）。
- **押金**：续租不额外收押金，复用主单押金（D12）。

### 4.2 支付流程（变更2：单渠道）

```
支付页（主单 UNPAID 或 续租子单 UNPAID）
  → 展示应付金额（主单=租金+押金；续租单=续租租金，押金 0）
  → 支付方式 radio 三选一（变更2，单渠道）：
       □ 2C2P        → 跳转/模拟网关
       □ 钱包余额    → 校验可用余额 ≥ 应付，不足则禁用并提示「余额不足，去充值」
       □ 线下支付    → 生成挂账凭证（选定即视为完成，无需核销，ADR-0001）
  → 确认支付
  → 模拟支付loading（复用 showRentLoading + yadi-loading）
  → 成功 → 订单/续租单 PAID → toast → 返回订单详情
  → 失败（如钱包不足被拦截）→ toast「支付失败」
```

- **不跨渠道拆分配额**（变更2）：用户选一种方式付清全额，无「2C2P 付 X + 钱包付 Y」的拆分 UI。
- 与平台组合支付的关系：平台侧仍可能按业务把一笔订单拆多渠（如改价含线下挂账），但**用户在 C 端只感知单一支付方式**。

### 4.3 改价确认流程（C 端只读 + 立即支付）

```
平台发起改价（ADR-0001 / 0004：价格 + 渠道分配，沉淀 paySplitAudits 前后快照）
  → 向用户推送「订单已改价」APP 消息（ADR-0001 强制）
  → 用户端订单详情：
       · 费用明细展示改价后新租金/新押金（红字提示「已改价」）
       · 「改价记录」入口：多级表头「改价前 / 改价后」，各含租金/押金 + 渠道明细（ADR-0004）
       · 仅 CREATED 订单显示「立即支付」按钮
  → 点击「立即支付」
  → 支付页（变更2 单渠道）
       · 应付展示（**线上 + 线下挂账均展示**，用户确认「都需要展示」）：
            - 线上应付 Rp X（用户选单一方式支付此部分）
            - 线下挂账 Rp Y（平台已挂账，无需用户支付，仅展示让用户可以知悉全貌）
            - 合计 Rp Z = X + Y
       · 用户选单一支付方式（2C2P / 钱包余额 / 线下支付）付清线上应付 X
  → 支付成功 → PAID
```

- **支付原子性**：付款前对钱包做预校验，整笔原子（失败前不扣任何渠道，ADR-0001）。

### 4.4 退款查看流程（C 端仅查看）

```
平台发起退款（ADR-0003 / 0005）
  → 用户端「退款记录」入口（仅查看，不可操作）
  → 退款记录列表：
       · 一次退款主行：租金(IDR) / 押金(IDR) / 时间 / 操作人 / 原因 / 状态
       · 展开：按渠道明细（各渠道退多少，单一币种 IDR，ADR-0003 缺口2）
  → 结束订单场景：仅退押金（ADR-0005），租金区不展示
```

- **结束订单仅退押金**：押金仅主订单持有，续租单无押金（ADR-0005）。

### 4.5 自动续租开关流程

```
订单详情 → 自动续租开关（opt-in，默认关闭，D18d）
  → 开启 → 到期日(rentEnd)当天系统自动生成续租单（AUTO 模式）并从钱包扣款
       · 订阅式无限循环，停止条件：用户关闭 / 订单终态 / 连续失败达阈值(默认3) / 用户手动续租让行
       · 扣款失败 → 自动取消续租单（CANCELLED 留痕），原订单正常结束（D18c）
       · 不收押金（复用主单押金，D18c）
  → 扣款前 N 天推送「扣款前提醒」（D18d）
  → 扣款失败推送「扣款失败」（次数/阈值占位）
```

---

## 5. 页面与路由（沿用现有全屏覆盖模式）

| 页面 | 触发变量 | 入口 |
|------|----------|------|
| 订单详情 | `showOrderDetail` | 订单列表 → 选中订单 |
| 费用明细列表 | `showFeeDetail` | 订单详情 → 「费用明细」入口（参考商户端 feedetail 子页列表形态） |
| 续租（方案选择） | `showRenewPlan` | 订单详情 → 续租（复用 `showPeriodPicker`/`showDatePicker`） |
| 支付页 | `showPayment` | 续租确认后 / 订单详情「立即支付」 |
| 改价记录 | `showPriceChange` | 订单详情 → 改价记录入口 |
| 退款记录 | `showRefundRecords` | 订单详情 → 退款记录入口 |
| 自动续租设置 | 内嵌于订单详情 | 订单详情 → 自动续租开关 |

---

## 6. 数据模型（Mock 参考，对齐平台 ADR）

### 续租子单（变更1 免审 · 可选方案）

```ts
renewOrder: {
  renewNo: string,              // 续租子单号
  parentOrderNo: string,       // 主单号
  renewMode: 'USER_APPLY',     // 用户端仅 USER_APPLY（免审）；AUTO 由系统生成
  plan: string,                // 用户所选租赁方案（续租时像下单一样可选，默认主单方案）
  newRentStart: string,        // = 主单 rentEnd
  newRentEnd: string,          // = rentEnd + 所选方案周期
  renewRentFee: number,        // 续租租金（按所选方案当期租金计算）
  renewDeposit: number,        // = 0（复用主单押金）
  payStatus: 'UNPAID' | 'PAID',// 仅支付状态流转（变更1 去掉审核态）
  createTime: string
}
```

### 改价快照（ADR-0004）

```ts
paySplitAudit: {
  splitNo: string,
  operator: string,
  createTime: string,
  reason: string,
  before: { rentFee: number, deposit: number, rentSplit: Channel[], depositSplit: Channel[] },
  after:  { rentFee: number, deposit: number, rentSplit: Channel[], depositSplit: Channel[] }
}
// Channel = { method: 'WALLET'|'2C2P'|'OFFLINE', amount: number }
```

### 退款记录（ADR-0003 / 0005）

```ts
refundRecord: {
  refundNo: string,
  rentRefund: number,          // 结束订单场景恒为 0（ADR-0005）
  depositRefund: number,
  time: string,
  operator: string,
  reason: string,
  status: 'refunding' | 'refunded',
  channels: { method: string, amount: number }[]  // 按原渠道明细
}
```

---

## 7. 钱包一致性核查（用户端 `wallet-plan.md` vs 平台 ADR）

> 用户要求：「钱包相关的已经实现了，检查下是否一致」。核查结论如下。

### 7.1 发现的问题

| 项 | 用户端 `wallet-plan.md`（已实现） | 平台 ADR（真源） | 结论 |
|---|---|---|---|
| **抵扣优先级** | L30：「先用**赠送余额**，再用**充值余额**」 | **D7**：「先扣**充值余额**，再扣**赠送余额**」 | ✅ **已确认以 ADR D7 为准：先充值后赠送**（实现需同步修正） |
| **字段命名** | `bonusBalance`（赠送余额） | `giftBalance`（赠送余额） | ⚠️ 命名不一致 |
| **提现范围** | 完整提现申请表单（设计决策已确认「需要」） | D13：提现仅支持充值余额 | ✅ 方向一致（需注意仅充值余额可提） |
| **冻结中间态** | 提及冻结栏位 | D13：冻结为内部中间态，不单列流水 | ✅ 一致 |
| **流水类型** | 充值池/赠送池各 4 类 | 充值池 RECHARGE/WITHDRAW/CONSUME/REFUND；赠送池 RECHARGE/DEDUCT/CONSUME/REFUND | ✅ 一致 |

### 7.2 建议

1. **抵扣优先级（已确认）**：以平台 ADR D7 为准，**先充值后赠送**。实现阶段需将 `wallet-plan.md` L30 与 `index.html` 抵扣逻辑由 bonus-first 改为 recharge-first（当前已实现为 bonus-first，属待修正项）。
2. **命名统一**：用户端 `bonusBalance` 建议与平台对齐为 `giftBalance`（或反之，需两端约定单一命名）。
3. 上述两项不影响页面结构，仅影响余额扣减顺序与字段命名，建议在进入 V1.4.0 实现前定稿。

---

## 8. 视觉与交互规范（沿用现有）

- 设计 Token：`--accent:#2B3F5F`、`--accent-2:#FF6A00`、`--bg:#F6F7F9`、`--surface:#FFFFFF`、`--muted:#999999`、`--border:#E5E5E5`。
- 支付方式 `radio` 沿用确认页支付网格（每行两个：第一排 2C2P + 线下支付，第二排 余额支付）。
- 续租方案选择器复用确认页 `showDatePicker`（租赁类型）/ `showPeriodPicker`（租赁方案）网格。
- 续租/支付/退款沿用现有全屏覆盖 + `show*` 触发变量模式。
- 推送 banner 复用现有 `push-wrap` 体系（续租提醒 / 扣款前提醒 / 扣款失败，见 `会话总结`）。
- 多语言：复用 `locales/*.json` 现有 `wallet` / `orders` / `common` 命名空间，新增 `renew` / `priceChange` / `refund` 命名空间（zh-CN / zh-HK / id / en 四语同步）。

---

## 9. 冻结内容

- 现有页面结构（首页、车辆、个人中心、钱包等 20+ 页面）保持不变。
- 导航结构（底部三 Tab）保持不变。
- 现有 Demo 操作面板、设计 Token、全局样式保持不变。
- 钱包核心能力（`wallet-plan.md` 已实现部分）保持不变，仅按 §7 做优先级/命名对齐。

---

## 10. 待用户确认

| # | 问题 | 建议 | 状态 |
|---|------|------|------|
| Q1 | 续租是否需要填写字段？ | **免审 + 可选租赁方案**：不填理由字段；但可像下单一样选择租赁方案（默认主单方案，可改） | ✅ 已确认（修订：由"免填表"调整为"可选方案"） |
| Q2 | 平台改价含线下挂账时，C 端「立即支付」应付额如何呈现？ | **线上应付 + 线下挂账两部分均展示**（合计 = X + Y，用户以单一方式支付线上部分，线下挂账由平台已挂账无需支付） | ✅ 已确认 |
| Q3 | 钱包抵扣优先级以哪方为准（recharge-first vs bonus-first）？ | **先充值后赠送（ADR D7）** | ✅ 已确认 |
| Q4 | 用户端字段命名是否对齐平台 `giftBalance`？ | 建议统一 | 待确认 |

---

*文档结束。规划确认后进入 V1.4.0 用户端设计实现阶段（变更影响评估 → 冻结内容 → 增量修改记录）。*
